import '@testing-library/jest-dom'
import {render, fireEvent, screen} from '@testing-library/react'
import Preview from 'components/lyrics/preview/preview'
import sampleSync from 'components/lyrics/syncing/sample_sync'

test('Drag and drop', () => {
  //render(<Preview syncedLyrics = {syncedLyrics}/>)
  //fireEvent.mouseDown(screen.queryByText('You\'re'))
  //fireEvent.mouseMove
})

test('VideoSkipForward', () => {
  
})
