import '@testing-library/jest-dom'
import {render, fireEvent, screen} from '@testing-library/react'
import PreviewLyric from 'components/lyrics/preview/preview_lyric'
import sampleSync from 'components/lyrics/syncing/sample_sync'

test('Pixel conversion', () => {
  const changeLyricById = (id, time) => {
    return time
  }
  <PreviewLyric duration={0} width={} />

)
test('VideoSkipForward', () => {

})
